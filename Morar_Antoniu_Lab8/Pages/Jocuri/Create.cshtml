@page
@model Morar_Antoniu_Lab8.Pages.Jocuri.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Joc</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Joc.Title" class="control-label"></label>
                <input asp-for="Joc.Title" class="form-control" />
                <span asp-validation-for="Joc.Title" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Joc.Author" class="control-label"></label>
                <input asp-for="Joc.Author" class="form-control" />
                <span asp-validation-for="Joc.Author" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Joc.Price" class="control-label"></label>
                <input asp-for="Joc.Price" class="form-control" />
                <span asp-validation-for="Joc.Price" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Joc.PublishingDate" class="control-label"></label>
                <input asp-for="Joc.PublishingDate" class="form-control" />
                <span asp-validation-for="Joc.PublishingDate" class="textdanger"></span>
            </div>
            <label asp-for="Joc.PublisherID" class="control-label">Publisher Name</label>
            <select asp-for="Joc.PublisherID" class="form-control" asp-items="ViewBag.PublisherID">
            </select>
            <span asp-validation-for="Joc.PublisherID" class="text-danger"></span>
            <div class="form-group">
                <div class="table">
                    <table>
                        <tr>
                            @{
                                int cnt = 0;
                                foreach (var cat in Model.AssignedCategoryDataList)
                                {
                                    if (cnt++ % 3 == 0)
                                    {
                                    @:</tr><tr>
                                    }
                                    @:<td>
                                        <input type="checkbox"
                                               name="selectedCourses"
                                               value="@cat.CategoryID"
                                               @(Html.Raw(cat.Assigned ? "checked=\"checked\"" : "")) />
                                        @cat.CategoryID @: @cat.Name
                                    @:</td>
                                }
                            @:</tr>
                            }
                        </table>
                    </div>
                </div>
                <div class="form-group">
                    <input type="submit" value="Create" class="btn btn-primary" />
                </div>
            </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
